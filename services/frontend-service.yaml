apiVersion: v1
kind: Service
metadata:
  name: frontendservice
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
      nodePort: 30080